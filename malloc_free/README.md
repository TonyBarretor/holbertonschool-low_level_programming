# malloc and free

malloc is a function that takes a single argument, this argument is going to be the size of memory allocated.

When you declared the malloc function you need to also declare the free function to liberate the memory you used with the function malloc.


