/**
 * int_strlean - returns the length of a string
 * @s: the strng
 * Return: 
 */

#include "main.h"

int strlen(char *s);
{
	int lenght = 0;

	for (; [length] != '\0'; length++);

	return length;

}
